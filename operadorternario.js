
// (condicao) ? 'valor verdadeiro' : 'valor falso';
// ? if 
// : else
const pontos = 90;

const resultado = (pontos >= 100) ? 'passou' : 'reprovou';

const userColor = 'red';

const corPadrao = userColor || 'blue';

console.log(resultado, corPadrao)



